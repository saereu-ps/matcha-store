{
  "name": "@matcha/auth-service",
  "version": "0.1.0",
  "private": true,
  "description": "OAuth2 Auth Service — token issuance, RBAC, user credentials",
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc",
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js",
    "test": "vitest run",
    "lint": "eslint src/",
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist coverage",
    "migrate": "prisma migrate deploy",
    "migrate:dev": "prisma migrate dev"
  },
  "dependencies": {
    "@matcha/shared-kernel": "workspace:*",
    "@matcha/telemetry": "workspace:*",
    "bcryptjs": "2.4.3",
    "express": "4.21.2",
    "zod": "3.24.1"
  },
  "devDependencies": {
    "@matcha/config": "workspace:*",
    "@types/bcryptjs": "2.4.6",
    "@types/express": "5.0.0",
    "@types/node": "^25.9.2",
    "tsx": "4.22.4",
    "typescript": "5.7.2",
    "vitest": "2.1.8"
  }
}
